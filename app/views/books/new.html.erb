<h2>New book</h2>
<%= form_with model:@book, local:true do |f| %>

<% if @book.errors.any? %>
  <%= @book.errors.count %> prohibited this book from being saved: %>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Body</h4>
      <%= f.text_area :body %>
    <div class="index-create-book">
      <%= f.submit "Create Book" %>
    </div>
  <% end %>